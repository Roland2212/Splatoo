import { Language } from '@core/interfaces/languages.interface';

export const CORE_LANGUAGES = [Language.EN, Language.UA];
export const CORE_LANGUAGES_FLAGS_MAP = new Map([
    ['en', 'us'],
    ['ua', 'ua'],
]);
